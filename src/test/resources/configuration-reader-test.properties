#
# Test file for reading properties. The values here should not be considered suggestions
# for real world configuration - these values are different than the defaults purely to
# test that reading configuration from an external property file works as expected.
#
# @see ConfigurationReaderTest#testReadFromTestPropertiesFile()

# sphinx server connection details
sphinx.dataSource.host=test.sphinx.org
sphinx.dataSource.port=9312

# pooling
sphinx.dataSource.testOnBorrow=true
sphinx.dataSource.testOnReturn=true
sphinx.dataSource.minIdle=0
sphinx.dataSource.maxIdle=10
sphinx.dataSource.maxTotal=10

